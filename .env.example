# Copy this to .env and paste your token. The server reads it; the browser never sees it.

# Fine-grained GitHub token with "Contents: Read-only" on the firmware repo.
# Used to list firmware releases and download release assets.
GITHUB_TOKEN=

# The private firmware repo, as owner/name.
GITHUB_REPO=K4HVH/medius-fw
